import { ActivityCurrencyFilter } from "./ActivityCurrencyFilter";
import { ActivityType } from "./ActivityType";
import { ActivityUserFilter } from "./ActivityUserFilter";
import { Blockchain } from "./Blockchain";
import { CollectionId } from "@rarible/types";
import { ItemId } from "@rarible/types";
export type ActivitySearchFilter = {
    blockchains?: Array<Blockchain>;
    types?: Array<ActivityType>;
    collections?: Array<CollectionId>;
    items?: Array<ItemId>;
    users?: ActivityUserFilter;
    currencies?: ActivityCurrencyFilter;
    from?: string;
    to?: string;
    fromBlockInclusive?: number;
    toBlockExclusive?: number;
};
