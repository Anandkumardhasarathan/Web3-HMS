import { BigNumberLike } from "@rarible/types";
import { CurrencyId } from "@rarible/types";
import { UnionAddress } from "@rarible/types";
export type Balance = {
    currencyId: CurrencyId;
    owner: UnionAddress;
    balance: BigNumberLike;
    decimal: BigNumberLike;
};
