import { CollectionId } from "@rarible/types";
import { Order } from "./Order";
export type ItemCollection = {
    id: CollectionId;
    name: string;
    bestBidOrder?: Order;
    bestSellOrder?: Order;
};
