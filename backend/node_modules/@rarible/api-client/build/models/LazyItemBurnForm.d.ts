import { ItemId } from "@rarible/types";
import { UnionAddress } from "@rarible/types";
export type LazyItemBurnForm = {
    id: ItemId;
    creators: Array<UnionAddress>;
    signatures: Array<string>;
};
