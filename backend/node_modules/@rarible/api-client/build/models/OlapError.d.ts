export type OlapError = {
    code: OlapErrorCode;
    message: string;
};
export declare enum OlapErrorCode {
    INVALID_INPUT = "INVALID_INPUT",
    NOT_FOUND = "NOT_FOUND",
    UNKNOWN = "UNKNOWN"
}
