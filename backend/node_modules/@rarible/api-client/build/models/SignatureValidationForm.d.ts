import { UnionAddress } from "@rarible/types";
export type SignatureValidationForm = {
    signer: UnionAddress;
    publicKey?: string;
    message: string;
    signature: string;
    algorithm?: string;
    weight?: number;
};
