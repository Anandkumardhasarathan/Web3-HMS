import type { Aptos } from "@aptos-labs/ts-sdk";
import type { BigNumber } from "@rarible/types";
export declare class AptosBalance {
    readonly aptos: Aptos;
    constructor(aptos: Aptos);
    getAptosBalance({ address }: {
        address: string;
    }): Promise<BigNumber>;
}
