import type { SupportedNetwork } from "../domain";
export declare const CONFIG: Record<SupportedNetwork, AddressConfig>;
export type AddressConfig = {
    marketplaceAddress: string;
    feeZeroScheduleAddress: string;
    raribleDropMachineAddress: string;
};
export declare function getEnvConfig(network: SupportedNetwork): AddressConfig;
