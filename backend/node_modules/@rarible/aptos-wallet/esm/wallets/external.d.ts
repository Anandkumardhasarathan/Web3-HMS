import type { AptosTransaction, AptosWalletInterface, ExternalAccount } from "../domain";
export declare class AptosSdkWallet implements AptosWalletInterface {
    readonly account: ExternalAccount;
    constructor(account: ExternalAccount);
    signMessage(msg: string, options?: {
        nonce: string;
    }): Promise<{
        signature: any;
        message: string;
    }>;
    getAccountInfo(): Promise<{
        address: string;
        publicKey: any;
        network: import("../domain").Network;
    }>;
    getPublicKey(): Promise<any>;
    signAndSubmitTransaction(payload: AptosTransaction): Promise<{
        hash: string;
    }>;
}
