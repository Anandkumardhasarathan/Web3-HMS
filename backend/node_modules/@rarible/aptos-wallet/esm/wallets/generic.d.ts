import type { Account as GenericAccount, Aptos } from "@aptos-labs/ts-sdk";
import type { AptosTransaction, AptosWalletInterface } from "../domain";
import { Network } from "../domain";
export declare class AptosGenericSdkWallet implements AptosWalletInterface {
    readonly aptos: Aptos;
    readonly account: GenericAccount;
    constructor(aptos: Aptos, account: GenericAccount);
    signMessage(msg: string): Promise<{
        message: string;
        signature: string;
    }>;
    getAccountInfo(): Promise<{
        address: string;
        publicKey: string;
        network: Network;
    }>;
    signAndSubmitTransaction(payload: AptosTransaction): Promise<{
        hash: string;
    }>;
}
export { GenericAccount };
