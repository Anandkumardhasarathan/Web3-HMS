import type { WalletCore } from "@aptos-labs/wallet-adapter-core";
import type { AptosTransaction, AptosWalletInterface } from "../domain";
export declare class AptosWalletCore implements AptosWalletInterface {
    readonly wallet: WalletCore;
    constructor(wallet: WalletCore);
    signMessage(message: string, options?: {
        nonce: string;
    }): Promise<{
        signature: string;
        message: string;
    }>;
    getAccountInfo(): Promise<{
        address: string;
        publicKey: string;
        network: import("../domain").Network;
    }>;
    signAndSubmitTransaction(payload: AptosTransaction): Promise<{
        hash: string;
    }>;
}
export type { WalletCore };
