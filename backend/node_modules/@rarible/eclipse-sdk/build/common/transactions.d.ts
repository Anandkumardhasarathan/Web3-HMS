import type { Commitment, Connection, PublicKey, TransactionInstruction } from "@solana/web3.js";
import { Transaction } from "@solana/web3.js";
import type { SolanaSigner } from "@rarible/solana-common";
import type { TransactionOrVersionedTransaction } from "@rarible/solana-common/src";
import type { DebugLogger } from "../logger/debug-logger";
import type { TransactionResult } from "../types";
export interface ITransactionPreparedInstructions {
    instructions: TransactionInstruction[];
    signers: SolanaSigner[];
    orderId?: PublicKey;
}
export declare function sendTransactionWithRetry(connection: Connection, wallet: SolanaSigner, instructions: TransactionInstruction[], signers: SolanaSigner[], commitment: Commitment, logger?: DebugLogger): Promise<TransactionResult>;
export declare function sendSignedTransaction({ signedTransaction, initialTransaction, connection, timeout, }: {
    signedTransaction: TransactionOrVersionedTransaction;
    initialTransaction: Transaction;
    connection: Connection;
    sendingMessage?: string;
    sentMessage?: string;
    successMessage?: string;
    timeout?: number;
}, logger?: DebugLogger): Promise<TransactionResult>;
