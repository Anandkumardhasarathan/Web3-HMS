import type { Commitment, Connection } from "@solana/web3.js";
import type { PublicKey } from "@solana/web3.js";
import { BigNumber } from "@rarible/utils";
export interface IEclipseBalancesSdk {
    getBalance(publicKey: PublicKey, options?: {
        commitment?: Commitment;
    }): Promise<BigNumber>;
    getTokenBalance(owner: PublicKey, mint: PublicKey): Promise<BigNumber>;
}
export declare class EclipseBalancesSdk implements IEclipseBalancesSdk {
    private readonly connection;
    constructor(connection: Connection);
    getBalance(publicKey: PublicKey, options?: {
        commitment?: Commitment;
    }): Promise<BigNumber>;
    getTokenBalance(owner: PublicKey, mint: PublicKey, options?: {
        commitment?: Commitment;
    }): Promise<BigNumber>;
}
