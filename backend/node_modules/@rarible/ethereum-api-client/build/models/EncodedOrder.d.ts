import { EVMAddress } from "@rarible/types";
import { SignMessage } from "./SignMessage";
import { Word } from "@rarible/types";
export type EncodedOrder = {
    orderHash: Word;
    signHash: Word;
    signMessage: SignMessage;
    transferProxyAddress?: EVMAddress;
};
