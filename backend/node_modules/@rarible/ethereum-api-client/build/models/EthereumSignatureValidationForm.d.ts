import { Binary } from "@rarible/types";
import { EVMAddress } from "@rarible/types";
export type EthereumSignatureValidationForm = {
    signer: EVMAddress;
    message: string;
    signature: Binary;
};
