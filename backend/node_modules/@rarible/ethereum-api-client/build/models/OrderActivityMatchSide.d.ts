import { Asset } from "./Asset";
import { EVMAddress } from "@rarible/types";
import { Word } from "@rarible/types";
export type OrderActivityMatchSide = {
    maker: EVMAddress;
    hash: Word;
    asset: Asset;
    type?: OrderActivityMatchSideType;
};
export declare enum OrderActivityMatchSideType {
    SELL = "SELL",
    BID = "BID"
}
