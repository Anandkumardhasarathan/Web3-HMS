import { ethers } from "ethers";
export declare function encodeParameters(types: any[], params: any[]): string;
export declare function decodeParameters(types: any[], data: string): ethers.utils.Result;
