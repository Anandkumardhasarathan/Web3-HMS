import { CONFIGS, flowCollectionsConfig } from "../../config/config";
import { getCollectionData } from "./index";
export function getCollectionConfig(network, collection) {
    var _a = getCollectionData(collection), name = _a.name, address = _a.address;
    var map = {};
    var config = flowCollectionsConfig[name];
    config.contractsNames.forEach(function (k) {
        map[k] = address;
    });
    return {
        map: Object.assign(map, CONFIGS[network].mainAddressMap),
        address: address,
        name: name,
        config: config,
    };
}
