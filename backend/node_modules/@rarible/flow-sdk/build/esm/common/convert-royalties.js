import { fixAmount } from "./fix-amount";
export function convertRoyalties(royalties) {
    return royalties.map(function (royalty) { return ({
        fields: [
            { name: "address", value: royalty.account },
            { name: "fee", value: fixAmount(royalty.value) },
        ],
    }); });
}
