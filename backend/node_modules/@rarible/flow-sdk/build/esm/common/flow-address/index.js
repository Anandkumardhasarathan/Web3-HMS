import { toFlowAddress } from "@rarible/types";
import { NON_FUNGIBLE_CONTRACTS } from "../../types";
export function parseContractAddress(x) {
    var _a = x.split("."), address = _a[1], name = _a[2];
    if (!address || !name) {
        throw new Error("Invalid contract address");
    }
    else if (NON_FUNGIBLE_CONTRACTS.indexOf(name) === -1) {
        throw new Error("Unknown or not a non-fungible contract name: ".concat(name));
    }
    return {
        address: toFlowAddress(address),
        name: name,
    };
}
