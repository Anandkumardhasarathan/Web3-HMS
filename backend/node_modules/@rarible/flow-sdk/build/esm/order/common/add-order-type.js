import { __assign } from "tslib";
export function addOrderType(order) {
    var make = order.make, take = order.take;
    if (take["@type"] === "fungible") {
        return __assign(__assign({}, order), { type: "LIST" });
    }
    else if (make["@type"] === "fungible") {
        return __assign(__assign({}, order), { type: "BID" });
    }
    throw new Error("Invalid order. Can't find fungible asset in make and take fields. make asset type is: ".concat(make["@type"], ", take asset type is ").concat(take["@type"]));
}
