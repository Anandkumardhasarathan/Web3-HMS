import { __assign } from "tslib";
import { toBn } from "@rarible/utils";
import { toBigNumberLike } from "@rarible/types";
export function calculateUpdateOrderSaleCuts(oldPrice, price, fees) {
    return fees.map(function (fee) {
        return __assign(__assign({}, fee), { value: toBigNumberLike(toBn(fee.value).div(toBn(oldPrice)).multipliedBy(toBn(price)).decimalPlaces(8).toString()) });
    });
}
