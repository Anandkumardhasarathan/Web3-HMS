import { toBigNumberLike } from "@rarible/types";
import { toBn } from "@rarible/utils";
import { CONFIGS } from "../config/config";
/**
 * returns fees object with value format in basis points 10000
 * @param network
 */
export function getProtocolFee(network) {
    return {
        buyerFee: CONFIGS[network].protocolFee,
        sellerFee: CONFIGS[network].protocolFee,
    };
}
/**
 * returns fees object with value in format floating points
 * @param network
 */
getProtocolFee.percents = function (network) {
    var _a = CONFIGS[network].protocolFee, account = _a.account, value = _a.value;
    var percentsValue = toBigNumberLike(toBn(value).div(10000).toString());
    return {
        buyerFee: { account: account, value: percentsValue },
        sellerFee: { account: account, value: percentsValue },
    };
};
