import { getVaultInitTx, vaultOptions } from "../../init-vault";
import { storefrontInit } from "../../storefront-init";
import { getListTxCode } from "../common/list";
import { garageImports, garagePreparePartOfInit } from "./init";
export var getGarageListTxCode = function (collection, currency) {
    var preparePreHookCode = "\n    ".concat(currency === "USDC" ? getVaultInitTx(vaultOptions["FiatToken"]) : "", "\n    ").concat(garagePreparePartOfInit, "\n    ").concat(storefrontInit, "\n  ");
    return getListTxCode(collection, { additionalImports: garageImports, preparePreHookCode: preparePreHookCode });
};
