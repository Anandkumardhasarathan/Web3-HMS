import { fixAmount } from "../../common/fix-amount";
export function prepareFees(fees) {
    return fees.map(function (f) { return ({
        key: f.account,
        value: fixAmount(f.value.toString()),
    }); });
}
