import { __awaiter, __generator } from "tslib";
import { runScript } from "../common/transaction";
import { getBalanceCode } from "../tx-code-store/balance";
import { CONFIGS } from "../config/config";
export function getFungibleBalance(fcl, network, address, currency) {
    return __awaiter(this, void 0, void 0, function () {
        var params;
        return __generator(this, function (_a) {
            if (fcl) {
                params = getBalanceCode(fcl, currency, address);
                return [2 /*return*/, runScript(fcl, params, CONFIGS[network].mainAddressMap)];
            }
            throw new Error("Fcl is required for balance request");
        });
    });
}
