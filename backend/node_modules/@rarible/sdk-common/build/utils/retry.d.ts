export declare function retry<T>(num: number, del: number, thunk: () => Promise<T>): Promise<T>;
export declare function delay(num: number): Promise<void>;
/**
 * Retry with conditions (for catching and skipping special errors)
 * @param num attempts amount
 * @param del delay
 * @param thunk retrying function
 * @param conditionCallback if callbacks returns true, proceed retrying
 */
export declare function conditionalRetry<T>(num: number, del: number, thunk: () => Promise<T>, conditionCallback: (error: any) => boolean): Promise<T>;
