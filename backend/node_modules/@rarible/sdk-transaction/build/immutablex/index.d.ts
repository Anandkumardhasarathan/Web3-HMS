import { Blockchain } from "@rarible/api-client";
import type { IBlockchainTransaction } from "../domain";
export declare class BlockchainImmutableXTransaction implements IBlockchainTransaction {
    transaction: number | undefined;
    blockchain: Blockchain;
    constructor(transaction: number | undefined);
    hash(): string;
    wait(): Promise<{
        blockchain: Blockchain;
        hash: string;
    }>;
    getTxLink(): string;
    get isEmpty(): boolean;
}
