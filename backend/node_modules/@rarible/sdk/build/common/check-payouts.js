"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkPayouts = void 0;
const build_1 = require("@rarible/logger/build");
function checkPayouts(payouts) {
    if (payouts === null || payouts === void 0 ? void 0 : payouts.length) {
        const payoutsAmount = payouts.reduce((acc, p) => (acc = acc + +p.value), 0);
        if (payoutsAmount !== 10000) {
            throw new build_1.Warning(`Sum of the values of Payouts objects should be equal to 10000 basis points, passed=${payoutsAmount}`);
        }
    }
}
exports.checkPayouts = checkPayouts;
