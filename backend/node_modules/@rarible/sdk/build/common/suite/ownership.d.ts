import type { Ownership, OwnershipId } from "@rarible/api-client";
import type { IRaribleSdk } from "../../domain";
export declare class OwnershipTestSuite {
    private readonly sdk;
    constructor(sdk: IRaribleSdk);
    waitForOwnership: (ownershipId: OwnershipId, predicate?: (value: Ownership) => boolean) => Promise<Ownership>;
    waitForNewOwnership: (itemId: string, nextOwner: string, predicate?: (value: Ownership) => boolean) => Promise<Ownership>;
}
