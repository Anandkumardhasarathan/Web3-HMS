import type { Maybe, UnionAddress } from "@rarible/types";
import type { BigNumberValue } from "@rarible/utils";
import type { SolanaWallet } from "@rarible/sdk-wallet";
import type { EclipseSdk } from "@rarible/eclipse-sdk";
import type { RequestCurrency } from "../../common/domain";
import type { IApisSdk } from "../../domain";
export declare class EclipseBalance {
    readonly sdk: EclipseSdk;
    readonly wallet: Maybe<SolanaWallet>;
    private readonly apis;
    constructor(sdk: EclipseSdk, wallet: Maybe<SolanaWallet>, apis: IApisSdk);
    getBalance(address: UnionAddress, currency: RequestCurrency): Promise<BigNumberValue>;
}
