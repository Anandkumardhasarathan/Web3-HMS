import type { IRaribleInternalSdk } from "../../domain";
export declare function createUnionSdk(evm: IRaribleInternalSdk, flow: IRaribleInternalSdk, tezos: IRaribleInternalSdk, solana: IRaribleInternalSdk, eclipse: IRaribleInternalSdk, immutablex: IRaribleInternalSdk, aptos: IRaribleInternalSdk): IRaribleInternalSdk;
