"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseItemId = void 0;
const types_1 = require("@rarible/types");
function parseItemId(itemId) {
    const split = itemId.split(":");
    if (split.length < 2) {
        throw new Error(`Unable to parse ItemId: ${itemId}`);
    }
    const [contract, tokenId] = split;
    return { contract: (0, types_1.toEVMAddress)(contract), tokenId };
}
exports.parseItemId = parseItemId;
