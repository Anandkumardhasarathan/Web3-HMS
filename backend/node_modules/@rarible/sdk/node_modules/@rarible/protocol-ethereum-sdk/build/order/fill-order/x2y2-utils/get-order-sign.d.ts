import type { BigNumber, EVMAddress } from "@rarible/types";
import type { RaribleEthereumApis } from "../../../common/apis";
export declare class X2Y2Utils {
    static SELL_OP: import("@rarible/types").BigNumberLike;
    static getOrderSign(apis: RaribleEthereumApis, request: {
        sender: EVMAddress;
        orderId: BigNumber;
        currency: EVMAddress;
        price: BigNumber;
    }): Promise<string>;
}
