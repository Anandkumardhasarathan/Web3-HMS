import type { SolanaSignature, SolanaSigner, TransactionOrVersionedTransaction } from "@rarible/solana-common";
import type { PublicKey, Transaction } from "@solana/web3.js";
import { Keypair } from "@solana/web3.js";
export declare class SolanaKeypairWallet implements SolanaSigner {
    readonly keyPair: Keypair;
    static fromKeypair(keyPair: Keypair): SolanaKeypairWallet;
    static fromKey(secret: Uint8Array | string): SolanaKeypairWallet;
    static fromSeed(seed: Uint8Array | undefined): SolanaKeypairWallet;
    readonly publicKey: PublicKey;
    private constructor();
    signTransaction: (tx: TransactionOrVersionedTransaction) => Promise<Transaction>;
    signAllTransactions: (txs: TransactionOrVersionedTransaction[]) => Promise<Transaction[]>;
    signMessage: (message: Uint8Array | string) => Promise<SolanaSignature>;
}
