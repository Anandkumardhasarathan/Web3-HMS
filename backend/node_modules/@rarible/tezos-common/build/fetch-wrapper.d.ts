import { RequestInit, Response } from "node-fetch";
import { Config } from "./types";
export declare type FetchOptions = RequestInit & {
    defaultErrorCode?: NetworkErrorCode;
};
export declare function fetchWrapper(url: string, options?: FetchOptions): Promise<Response>;
export declare enum NetworkErrorCode {
    TEZOS_NETWORK_ERR = "TEZOS_NETWORK_ERR",
    TEZOS_EXTERNAL_ERR = "TEZOS_EXTERNAL_ERR"
}
export declare function fetchAPI(relativeUrl: string, options: FetchOptions & {
    config: Config;
}): Promise<Response>;
