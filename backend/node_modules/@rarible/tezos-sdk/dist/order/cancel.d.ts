import { Provider, TransactionArg, OperationResult } from "@rarible/tezos-common";
import { OrderForm } from "./utils";
export declare function cancel_arg(provider: Provider, order: OrderForm): Promise<TransactionArg>;
export declare function cancel(provider: Provider, order: OrderForm): Promise<OperationResult>;
