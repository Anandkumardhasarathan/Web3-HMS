import { isRealBlockchainSpecified } from "../../blockchains/index.js";
export function toItemId(value) {
    if (!isRealBlockchainSpecified(value)) {
        throw new Error(`Not an ItemId: ${value}`);
    }
    return value;
}
